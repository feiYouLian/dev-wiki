# 2026-09-15

## 整理 gitTest 文档到 VitePress 项目
- 将 `D:\giteeRepo\gitTest` 的 Markdown 笔记按主题整合进本项目的 VitePress 文档站（`docs/`）。
- 新增/迁移分类目录：`git/`、`docker/`、`bigdata/`、`efk/`、`algorithm/`、`go/`、`java/HashMap/`，根级 `SSO.md`，以及总览页 `notes.md`。
- 关键处理：
  - 保持目录结构原样拷贝，使 `./img/*.png` 等相对图片链接在 VitePress 中正常解析。
  - `go/`、`java/` 下的纯源码（`.go`/`.java`）**未**纳入文档站，仅取说明文档（go 的 README、java/HashMap）。
  - 源文件 `git命令大全.md` 引用了不存在的 `./img/0.jpg`，改为文字说明。
  - 各分类索引 `README.md` 改名为 `index.md`（VitePress 死链检查只认 `index.md` 作为目录根）。
  - `git/img/README.md`（图片目录误带的说明）迁移为 `git/resources.md` 并修正链接。
  - 更新 `docs/.vitepress/config.mts` 的 nav（新增“笔记”）与 sidebar（各分类分组）。
- 验证：`vitepress build docs` 构建成功，零死链。
- 环境注意：本机 Bash 的 coreutils 缺失（ls/find/head 不可用）、PowerShell 不回显 stdout；用 Glob 验证文件落盘，PowerShell 把输出写入日志文件再用 Read 读取。

## 第二轮：补迁移 spring 模块 + Go/Java 源码 + 修构建
- spring 模块在 gitTest 中原本是 git 跟踪文件但未在工作区：用 `git checkout HEAD -- spring` 从版本库恢复（约 23 个文件，含 `imgs/beanLifeCycle.png`，并删除原 `.html`）。
- 迁移 Go/Java 纯源码到 `docs/public/code/`（`go/`: goweb/market-task/sync/test/websocket；`java/`: BinaryTree/DateUtils/DynamicProxy/core/*），通过 `config.mts` 的 `ignoreDeadLinks: [/^\/code\//]` 放行静态资源死链，新建 `go/code.md`、`java/code.md` 以 `/code/...` 绝对路径引用。
- 新建 `docs/java/index.md` 作为 Java 总览页（原 spring 里 `../java/README.md` 链接失效，gitTest 中也无此文件）。
- 构建报错修复：
  1. `spring/spring内置功能.md:577` Vue 插值未闭合（`#{{user.age} + 5}`）：整句包 `<span v-pre>` 让 Vue 原样输出 `{{ }}`。
  2. 死链 `./../java/README` → 改为 `/java/`；死链 `./192.168.1.100` → 改为 `http://192.168.1.100`。
  3. sidebar Java 分组补充“Java 学习笔记”→`/java/` 入口。
- 最终验证：`vitepress build docs` 成功（exit 0，build complete，零死链零报错；仅 `profile/conf/options/glide` 高亮语言回落 txt 的无害告警）。
- 清理：删除了本轮产生的临时日志 build.log/build_utf8.log/spring_*.log（注意 safe-delete 钩子首次 trash 失败、重试成功）。
